{
  "name": "python-enumerate",
  "version": "1.0.0",
  "description": "Enumerate through an Iterable and get the index",
  "main": "build/index.js",
  "scripts": {
    "publish-patch": "tsc && tsc -d && git commit -am 'Patch publish' && npm version patch && npm publish",
    "publish-minor": "tsc && tsc -d && git commit -am 'Minor publish' && npm version minor && npm publish",
    "publish-major": "tsc && tsc -d && git commit -am 'Major publish' && npm version major && npm publish"
  },
  "author": "Louis Béranger",
  "license": "CC BY-SA 4.0",
  "repository": "https://github.com/alkihis/python-enumerate",
  "bugs": "https://github.com/alkihis/python-enumerate/issues",
  "keywords": [
    "python",
    "enumerate"
  ]
}
